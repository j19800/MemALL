"""Lark/Feishu IM integration — multi-bot bidirectional IM.

Modules:
    credentials — Store/load bot credentials per agent (JSON).
    client      — Multi-bot tenant_access_token management + send/reply.
    webhook     — Event subscription receiver for real-time @mentions.
"""